node_modules
dist
dist-ssr
*.local
.DS_Store

# Rust / Tauri
target/
src-tauri/target/
Cargo.lock.tmp

# TypeScript incremental build info
*.tsbuildinfo

# tsc -b sin outDir emite estos junto a vite.config.ts al correr `npm run
# build` (preexistente, no depende de estos cambios) — no son fuente.
vite.config.js
vite.config.d.ts
